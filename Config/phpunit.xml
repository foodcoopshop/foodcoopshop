<phpunit>
  <filter>
    <!-- adds non-existing code coverage for files that are not called in tests -->
    <whitelist processUncoveredFilesFromWhitelist="true">
      <directory suffix=".php">../Controller</directory>
    </whitelist>
  </filter>
</phpunit>